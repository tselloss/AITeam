---
description: Route a request through the AITeam roster, starting with the CEO's intake.
---

Invoke the AITeam CEO subagent via the Agent tool right now — do not attempt the
request below yourself first. Use whichever `subagent_type` the current
available-agent-types listing shows for the AITeam `ceo` role: if this project
defines its own copy under `.claude/agents/ceo.md` that's just `ceo`; if `ceo` is
only available from the installed plugin it will be listed as `aiteam:ceo`.

Pass the request through exactly as given, and let `ceo` triage, route, and drive
it through the rest of the roster to completion per `docs/team-protocol.md`.

Request: $ARGUMENTS
